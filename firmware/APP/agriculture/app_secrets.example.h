#ifndef _APP_SECRETS_EXAMPLE_H
#define _APP_SECRETS_EXAMPLE_H

/* Copy to app_secrets.h and fill locally. Never commit the real password. */
#define APP_WIFI_PASSWORD  "replace-with-hotspot-password"

#endif
